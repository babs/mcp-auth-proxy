# Keycloak demo realm

This realm is for local Docker Compose testing only. It contains a
hardcoded client secret and demo user password; rotate every credential
and review the client, redirect URI, hostname, and TLS settings before
using Keycloak for any real deployment.
